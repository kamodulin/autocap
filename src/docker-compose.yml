version: '3.8'

services:
  api:
    container_name: flask-api
    build: ./api
    volumes:
      - ./api/:/app/
  frontend:
    container_name: react-frontend
    build: ./frontend
    command: npm start
    volumes:
      - ./frontend/:/app/
      - node_modules:/app/node_modules
    ports:
      - 3000:3000

volumes:
  node_modules:
